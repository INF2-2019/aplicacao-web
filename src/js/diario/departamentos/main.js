// Controller.consulta();

Tabela.insere({ id: 1, idCampi: 2, nome: "Decom" });

document.addEventListener('DOMContentLoaded', function() {
  var elems = document.querySelectorAll('.modal');
  var instances = M.Modal.init(elems);
});
